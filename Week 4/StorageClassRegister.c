#include <stdio.h>
#include <stdlib.h>

int main(){

    register int weight;
    //int* ptr = &weight;
    // will give a error as we cant get a 
    // memory location when dealing with cpu register 


    return 0;
}