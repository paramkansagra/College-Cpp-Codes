#include <stdio.h>

int main(){

    // lets do a for loop from 1 to n

    int n;
    printf("input n ");
    scanf("%d",&n);

    // now printing
    for(int i=1;i<=n;i++){ // same as cpp
        printf("%d ",i);
    }printf("\n");

    return 0;
}